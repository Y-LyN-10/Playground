{
  "name": "weather-here-and-now",
  "version": "1.0.0",
  "description": "For those, who are too lazy to get up and take a look through the window, so they(me) are looking in the terminal for that kind of information",
  "main": "weather.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^0.19.0",
    "external-ip": "^0.2.4",
    "request": "^2.65.0",
    "satelize": "^0.1.2"
  }
}
